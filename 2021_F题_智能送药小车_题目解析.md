# 2021 年全国大学生电子设计竞赛 F 题——智能送药小车题目解析

> 原题文件：`D:\Documents\01交大\实验室实习\19-25历年小车题目\2021_F题_智能送药小车.pdf`
>
> 本文目标：将原题拆解为可实现、可测试的系统需求，并结合当前 `2026_TICar` 工程分析已有基础、功能缺口和推荐开发顺序。

---

## 1. 题目本质

这道题并不只是“沿线行驶”，而是一套小型自主运输系统。完整任务同时包含：

1. **目标输入**：在药房识别参赛者手持的 1～8 号病房数字。
2. **载荷检测**：检测约 200 g 药品已经装载，随后自动出发。
3. **自主导航**：沿走廊中央红线行驶，识别岔路和沿途病房编号。
4. **动态寻址**：3～8 号病房在测试时随机布置，不能只把“数字”和“固定路线”硬编码绑定。
5. **精确停车**：车头投影必须进入药房或病房的门口区域。
6. **装卸状态检测**：到达病房后等待人工卸药，并在检测到卸载后自动返航。
7. **状态指示**：病房等待卸药时亮红灯，返抵药房后亮绿灯；双车任务还需要黄灯。
8. **双车协同**：通过无线通信实现等待、放行和远程启动，并避免碰撞、错车和超车。
9. **时间优化**：单程运输和返程分别要求小于 20 s，双车任务总时间要求不超过 60 s。

因此，本题可以概括为：

> **数字视觉识别 + 红线循迹 + 路口拓扑判断 + 编码器/IMU 定位 + 载荷检测 + 状态机 + 双车无线调度。**

---

## 2. 场地与路线结构

### 2.1 场地元素

| 元素 | 题目规定 | 对系统的影响 |
|---|---|---|
| 地面 | 白色亚光喷绘布 | 视觉系统应适应白色背景和反光变化 |
| 走廊中心线 | 红色实线，宽约 1.5～1.8 cm | 小车的主要循迹目标 |
| 走廊墙体 | 黑色实线，宽约 1.5～1.8 cm | 不能压线或越界，也是视觉干扰源 |
| 门口区域 | 约 2 cm 黑白相间虚线围成 | 到达判定必须保证车头投影进入该区域 |
| 病房标识 | 黑色数字 1～8，边框 8 cm × 6 cm | 需要在药房和路线途中识别 |
| 数字与红线间距 | 数字边框距实线约 2 cm | 相机视场需同时覆盖路线和数字所在区域 |
| 远端并排数字间距 | 两个数字边框间约 2 cm | 远端需要区分相邻的两个方向标识 |

### 2.2 主要尺寸

由题目图 1 可读出的关键尺寸包括：

- 主要走廊宽度约 **30 cm**。
- 药房、近端病房和中部病房的横向支路长度约 **40 cm**。
- 远端区域中央路口到左右侧走廊的水平距离约 **60 cm**。
- 远端左右侧走廊宽度约 **30 cm**。
- 远端侧走廊在横向主路上、下方的纵向长度各约 **40 cm**。
- 药房至近端、近端至中部、中部至远端等主要纵向区段以约 **60 cm** 为量级。
- 门口虚线区域相对墙体局部外伸约 **5 cm**。

最长的远端单程路径为数米量级。20 s 的限制本身并不要求极端高速，真正消耗时间的是：

- 起步和停车；
- 路口减速；
- 数字识别确认；
- 90° 转向；
- 进入门口区域后的精确停车。

### 2.3 路线拓扑

场地可抽象为一张固定拓扑图：

| 区域 | 房间数量 | 编号是否固定 | 典型路线决策 |
|---|---:|---|---|
| 药房 | 1 个起终点 | 固定 | 出发、返航、最终停车 |
| 近端病房 | 2 个 | 1 号在左、2 号在右，固定 | 到达第一个横向路口后左转或右转 |
| 中部病房 | 2 个 | 3～8 中的两个，测试时随机 | 到达中部路口，读取左右标识后选择方向 |
| 远端病房 | 4 个 | 3～8 中剩余四个，测试时随机 | 先在远端中央路口选择左/右，再在侧走廊选择上/下 |

最重要的结论是：

> 近端 1、2 号可以直接按固定路线行驶；中部和远端的 3～8 号必须根据现场数字标识动态决定路径。

如果程序采用 `目标数字 -> 固定转向序列` 的简单查表，只能稳定完成 1、2 号，无法应对 3～8 号随机布置。

---

## 3. 完整工作流程

题目要求的单车标准流程如下：

| 阶段 | 小车动作 | 进入下一阶段的条件 |
|---|---|---|
| 复位 | 小车位于药房，车头朝向病房，所有执行器进入安全状态 | 复位完成 |
| 识别目标 | 识别手持的病房数字 1～8 | 连续多帧稳定得到同一数字 |
| 等待装药 | 保存目标编号，监测载荷 | 检测到约 200 g 药品装载完成 |
| 自动出发 | 关闭全部指示灯，开始运输计时 | 载荷信号稳定有效 |
| 自主运输 | 循迹、识别路口和病房号、执行路径决策 | 到达目标病房门口区域 |
| 等待卸药 | 停车并点亮红色指示灯 | 检测到药品已经卸载 |
| 自动返航 | 熄灭红灯，开始返程计时，沿原路线返回 | 到达药房门口区域 |
| 任务完成 | 停车，车头朝向药房，点亮绿色指示灯 | 保持停止，等待下一次复位 |

### 3.1 计时边界

计时起点不能理解错误：

- **运输计时**：装载药品后即刻开始。
- **返程计时**：卸载药品后即刻开始。
- 药房数字识别本身要求不超过 **20 s**，但它发生在装药和正式运输计时之前。
- 每项测试只测试一次，计时开始后不得人工干预。

程序应以载荷传感器的稳定边沿作为计时和状态跳转依据，而不是依赖人工按键再次启动。

---

## 4. 基本要求解析

### 4.1 近端病房

要求单车向固定的 1 号或 2 号病房送药并返回：

- 1 号和 2 号位置固定；
- 主要考查数字输入、装卸检测、基本循迹、一次 90° 转向、门口停车和返航；
- 运送时间小于 20 s；
- 返回时间小于 20 s。

这是最适合首先打通的完整闭环任务。

### 4.2 中部病房

要求单车向随机编号的中部病房送药并返回：

- 必须识别路口两侧的数字标识；
- 需要区分“当前看到目标数字”和“数字位于左侧还是右侧”；
- 若没有识别到目标，应继续驶向远端，而不能在中部误转；
- 运送和返回时间分别小于 20 s。

中部任务是验证“动态病房寻址”是否真正实现的关键项目。

### 4.3 远端病房

要求单车向随机编号的远端病房送药并返回：

- 路程最长；
- 需要经过更多路口；
- 至少包含中央纵向路口的左右选择和侧走廊的上下选择；
- 远端标识较密集，存在相邻数字误识别风险；
- 返程需要可靠记录或反推已走路径；
- 运送和返回时间分别小于 20 s。

远端任务的难点不是单纯提高车速，而是保证多次决策、转向和停车不出错。

---

## 5. 发挥部分解析

## 5.1 双车向同一中部病房送药

流程重点：

1. 小车 1 先送药并在目标病房亮红灯等待卸载。
2. 小车 2 后续启动，在自选暂停点停车并亮黄灯。
3. 暂停点处，小车 2 与小车 1 的车头中心沿红线距离不得小于 70 cm。
4. 小车 1 卸药后开始返航，同时无线通知小车 2。
5. 小车 2 收到通知后熄灭黄灯，继续前往病房。
6. 从小车 2 启动开始，到“小车 1 返回药房且小车 2 到达病房”为止统计总时间。
7. 小车 2 黄灯亮起期间的暂停时间不计入总时间。
8. 总时间超过 60 s，该项计 0 分。

该任务的实质是一个**单车道交通管制问题**。自选暂停点必须满足：

- 距离病房中的小车 1 至少 70 cm；
- 不能占据小车 1 的返程冲突路段；
- 小车 1 返程时不能与小车 2 在同一走廊错车；
- 尽量靠近目标病房，以缩短被放行后的剩余行程。

推荐把暂停点设置为一个经过实测的固定拓扑节点，并用编码器距离和地面特征双重确认，不建议只靠运行时间停车。

## 5.2 双车向不同远端病房送、取药

流程重点：

1. 小车 1 执行送药。
2. 小车 2 在药房识别自己的取药目标，但保持等待。
3. 小车 1 到达送药病房并完成卸载后开始返航。
4. 小车 1 同时向小车 2 发送“开始取药”指令。
5. 小车 2 收到有效指令后自动启动。
6. 小车 2 到达取药病房后停车并亮红灯。
7. 从小车 1 开始返航起，到“小车 1 返回药房且小车 2 到达取药病房”为止计时。
8. 总时间超过 60 s，该项计 0 分。

该任务必须在启动小车 2 前判断两车路线是否会产生冲突。由于题目禁止在同一走廊错车或超车，不能简单地在小车 1 一卸载就无条件放行小车 2。

建议通信消息至少携带：

- 发送车编号；
- 当前任务编号；
- 消息类型；
- 当前拓扑区域或已释放路段；
- 序号；
- 校验字段。

关键控制消息应采用“发送—确认—超时重发”机制，不能使用只发送一次的裸消息。

---

## 6. 强制约束与零分风险

### 6.1 机械和供电约束

- 小车尺寸不大于 **25 cm × 20 cm × 25 cm**。
- 只能使用普通车轮。
- 禁止使用履带、麦克纳姆轮等特殊结构。
- 两车均必须由电池供电。
- 允许两车无线通信。
- 场外不得设置附加电路或控制装置。

小车最大宽度 20 cm，而走廊宽度约 30 cm，理论上每侧只有约 5 cm 余量。实际设计还需考虑：

- 车身装配偏差；
- 转向时车角扫掠范围；
- 相机和传感器支架是否超出投影；
- 电机左右差异；
- 地面不平和轮胎打滑。

因此底盘不宜做到 20 cm 的极限宽度，且转向中心应尽量靠近车体几何中心。

### 6.2 光照约束

作品必须适应：

- 无阳光直射的自然光；
- 顶置多灯照明；
- 不得要求特殊补光环境。

可以使用车载补光，但不能把识别效果建立在赛场提供特殊灯光的前提上。视觉算法应针对曝光、白平衡、阴影和局部反光进行测试。

### 6.3 指示灯约束

> 任何一个指示灯处于点亮状态时，小车必须处于停止状态。

这意味着软件中不能把红、黄、绿灯当作普通运行状态提示。更安全的设计是：

1. 先下发主动制动；
2. 确认目标速度为 0，必要时确认编码器速度接近 0；
3. 再点亮对应指示灯；
4. 熄灯后才允许重新给出非零速度命令。

### 6.4 边界和碰撞规则

下列情况会被扣分：

- 小车投影落在黑色墙线上；
- 两车发生碰撞。

下列情况会使当前测试项直接计 0 分：

- 小车投影连续落在黑实线上超过 30 cm；
- 整车越过黑实线；
- 两车连续接触超过 5 s；
- 双车任务总时间超过 60 s。

因此，安全边界检测的优先级高于单纯提速。

### 6.5 操作约束

- 每项测试开始时只允许按一次复位键。
- 装药后必须自动启动。
- 卸药后必须自动返航。
- 计时开始后不得人工干预。
- 每项只测试一次，没有“失败后再试”的机会。

软件必须设计故障降级策略，避免偶发识别错误直接演变为越界或碰撞。

---

## 7. 评分结构与开发优先级

| 部分 | 分值 | 占 120 分比例 | 建议优先级 |
|---|---:|---:|---|
| 设计报告 | 20 | 16.7% | 与实物开发同步积累材料 |
| 基本要求（1）：近端 | 12 | 10.0% | 第一优先级 |
| 基本要求（2）：中部 | 18 | 15.0% | 第二优先级 |
| 基本要求（3）：远端 | 20 | 16.7% | 第三优先级 |
| 发挥（1）：同一中部病房双车协同 | 23 | 19.2% | 基本项稳定后开发 |
| 发挥（2）：不同远端病房送取药 | 21 | 17.5% | 双车最终目标 |
| 其他 | 6 | 5.0% | 最后优化 |
| **总分** | **120** | **100%** | 先可靠，再提速 |

基本要求共 50 分，设计报告 20 分。先完成稳定单车系统，可获得最多 70 分的主体基础，再进入双车协同。

推荐得分顺序：

1. 打通 1、2 号近端完整闭环；
2. 实现中部动态号码识别；
3. 实现远端多级路径决策；
4. 优化单程和返程时间；
5. 复制并标定第二辆车；
6. 完成双车通信和路权管理；
7. 最后再做附加功能和极限提速。

---

## 8. 推荐系统方案

## 8.1 系统分工

推荐采用“高层视觉 + 底层实时控制”的分层架构：

| 模块 | 推荐职责 |
|---|---|
| K230 | 数字 1～8 检测、数字方位判断、可选的红线/路口视觉识别 |
| MSPM0 | 电机控制、编码器测速、IMU 航向、CCD 循迹、载荷输入、指示灯、任务状态机 |
| CCD/线传感器 | 高频中心线偏差测量，负责直线段实时纠偏 |
| 编码器 | 距离估计、减速点、停车点和转向辅助判定 |
| IMU | 直线航向保持、90° 转向角度闭环、打滑时辅助纠偏 |
| 载荷传感器 | 检测装药和卸药 |
| 无线模块 | 双车状态、路权和启动指令传输 |

原则是：

- K230 不直接输出左右轮 PWM；
- MSPM0 不承担复杂图像识别；
- 每一层都应在上层数据超时或异常时进入安全停车，而不是继续使用陈旧数据。

## 8.2 数字识别

数字识别需要覆盖两个场景：

### 药房手持识别

- 目标纸张由参赛者手持，距离和角度存在变化；
- 识别时间不得超过 20 s；
- 识别结果必须在装药前锁定；
- 建议连续若干帧得到同一数字后才确认；
- 确认后应通过屏幕、蜂鸣器或调试输出让参赛者知道目标已锁定，但正式指示灯的使用必须服从“亮灯即停车”规则。

### 路线途中识别

途中不仅要识别数字类别，还要输出其空间位置：

- 数字值；
- 检测框中心坐标；
- 位于车体左侧还是右侧；
- 置信度；
- 连续稳定帧数；
- 当前所在拓扑路口。

单纯发送“识别到了数字 6”还不够，因为底盘必须知道 6 号标识位于哪个方向。

建议对目标房间号采用更低误判率的匹配策略：

1. 只保留与目标编号相同的检测框；
2. 限制检测框尺寸和图像区域；
3. 结合里程判断当前应出现哪一组房间标识；
4. 连续多帧确认；
5. 在决策窗口结束前没有可靠结果时停车或低速复扫，禁止盲目转向。

## 8.3 红线循迹与黑线干扰

题目中的中心线为红色，墙体为黑色。若使用灰度 CCD，两者都可能表现为低灰度区域，存在把墙线当作中心线的风险。

可采用以下措施：

- 限制 CCD 的实际视场，只覆盖车体前方中央区域，尽量不看到两侧黑墙；
- 对红线、黑线和白底分别采集灰度样本，验证红线在不同光照下是否可稳定分离；
- 直线段使用 CCD 高频纠偏；
- 路口识别使用摄像头颜色分割或宽视场线阵列；
- 结合编码器里程建立路口预期窗口，避免任意位置的异常线形触发转向；
- 线路丢失时限制搜索距离和速度，超时立即停车。

更稳妥的融合方式是：

- **CCD 负责连续控制；**
- **编码器负责“什么时候接近路口”；**
- **K230 负责“路口是什么、数字在哪边”；**
- **IMU 负责“转了多少角度”。**

## 8.4 90° 转向

不要只用固定延时完成转向。固定延时会受到以下因素影响：

- 电池电压；
- 载荷变化；
- 左右电机差异；
- 轮胎与地面的摩擦；
- 转向前车速；
- 地面局部褶皱。

推荐采用分阶段转向：

1. 路口前减速；
2. 到达预定转向中心后主动制动；
3. 使用编码器或 IMU 进行约 70°～85° 的粗转；
4. 低速继续转动并重新寻找目标红线；
5. 红线连续稳定居中后结束转向；
6. 清除转向积分和历史误差，再恢复巡航速度。

## 8.5 到达和停车判定

“看见目标数字”不等于“已经到达病房”。到达判定建议组合以下信息：

- 当前状态已经进入目标支路；
- 从转向点驶过指定编码器距离；
- 检测到门口虚线、线路终点或预期视觉特征；
- 车速已降到停车区间；
- 停车后车头投影位于门口区域。

应为每类房间分别标定停车距离：

- 近端左右门口；
- 中部左右门口；
- 远端上、下门口；
- 药房返航门口。

## 8.6 载荷检测

题目只要求检测约 200 g 药品是否装载/卸载，并不要求测量精确重量。

可选方案：

| 方案 | 优点 | 风险 |
|---|---|---|
| 微动开关 + 托盘 | 简单、实时、抗干扰、容易调试 | 需要合理设计托盘行程和触发力 |
| 薄膜压力传感器 | 结构薄、模拟量可调阈值 | 一致性、温漂和迟滞需要标定 |
| 小型称重传感器 + ADC | 可得到重量，阈值清晰 | 结构和软件较复杂，运动振动会影响读数 |
| 光电遮挡传感器 | 不直接受重量影响 | 药品摆放位置变化可能导致漏检 |

推荐优先使用机械微动开关或结构化托盘方案，并实现：

- 20～100 ms 去抖；
- 装载和卸载使用不同的稳定确认时间；
- 车辆运动时不因颠簸误判卸载；
- 传感器异常时保持停车。

## 8.7 路径记忆与返航

返航有两种主要实现方式：

### 方案 A：记录出发转向序列

例如记录：

- `左转`；
- `右转`；
- `直行通过某路口`。

返航时按逆序执行相反转向。

优点是简单、对随机数字布局适应性强。缺点是必须保证每次路口事件记录准确。

### 方案 B：建立动态房间映射

在出发途中记录：

- 当前拓扑节点；
- 左右侧数字；
- 目标房间方向；
- 已通过的路段。

返航时使用场地拓扑规划回药房。

该方案更适合双车协同，因为通信中可以直接传输拓扑位置和已释放路段。

推荐先实现方案 A 完成基本要求，再升级为方案 B 支持双车路权管理。

---

## 9. 推荐任务状态机

建议将比赛任务与现有调试模式分离，建立独立的比赛状态机。

| 状态 | 主要动作 | 安全条件 |
|---|---|---|
| `RESET` | 关闭电机和指示灯，清除历史任务 | 所有输出为安全状态 |
| `IDENTIFY_TARGET` | K230 识别手持数字 | 超过 20 s 未确认则报故障并保持停车 |
| `WAIT_LOAD` | 保存目标编号，等待载荷稳定有效 | 禁止电机启动 |
| `OUTBOUND_START` | 熄灭指示灯、复位里程和路线记录 | 载荷有效、目标有效、传感器在线 |
| `FOLLOW_MAIN` | 主走廊循迹 | 丢线或姿态异常时减速/停车 |
| `APPROACH_JUNCTION` | 降速，读取数字和路口形态 | 未可靠识别时不得盲转 |
| `TURN` | 编码器/IMU 粗转，红线细对准 | 转向超时进入故障 |
| `ENTER_ROOM` | 沿支路低速进入门口 | 防止冲出黑线边界 |
| `WAIT_UNLOAD` | 主动制动并亮红灯 | 电机必须保持停止 |
| `RETURN_PREPARE` | 检测卸载，熄灭红灯，装载返程路线 | 确认灯已熄灭后才允许运动 |
| `RETURNING` | 逆序执行路线并返回药房 | 继续执行边界和丢线保护 |
| `ARRIVE_PHARMACY` | 主动制动、确认停止、亮绿灯 | 保持停止直到复位 |
| `FAULT` | 关闭驱动或主动制动，记录故障 | 禁止自动恢复到高速运动 |

所有状态均应采用非阻塞方式推进，使程序可以持续处理：

- 急停或模式切换；
- K230 串口；
- 无线通信；
- 传感器超时；
- 电机故障；
- 调试信息。

---

## 10. 双车路权设计

双车协同的核心不只是“无线发送开始命令”，而是明确谁可以进入哪一段单车道。

可以把场地划分为若干互斥路段：

- 药房入口段；
- 药房至近端主走廊；
- 近端至中部主走廊；
- 中部至远端主走廊；
- 远端左侧区域；
- 远端右侧区域；
- 各病房支路。

任意时刻，一段存在迎面冲突可能的走廊只允许一辆车持有路权。

建议的无线状态包括：

| 消息 | 含义 |
|---|---|
| `READY` | 本车完成识别并准备执行任务 |
| `START` | 允许另一辆车启动 |
| `PAUSED` | 已到暂停点并停止，黄灯已亮 |
| `UNLOADED` | 送药车完成卸载，准备返航 |
| `SEGMENT_CLEAR` | 指定路段已经释放 |
| `ARRIVED` | 已到达目标病房或药房 |
| `ACK` | 已收到并接受对应命令 |
| `FAULT` | 本车故障，另一辆车不得继续进入冲突区域 |

安全原则：

- 收不到确认时宁可停车，不可假设对方已收到；
- 收到重复命令时不得重复执行启动或状态跳转；
- 通信超时后不得继续进入可能冲突的公共走廊；
- 每条关键消息应包含序号，避免旧消息触发新任务；
- 两车均应独立具备停车能力，不能依赖一辆车统一控制另一辆车的电机。

---

## 11. 时间预算与速度策略

单程 20 s 限制下，不应全程高速。建议按区域设置速度：

| 区域 | 建议策略 |
|---|---|
| 长直道 | 在循迹稳定后使用较高巡航速度 |
| 路口前 | 根据编码器里程提前减速 |
| 数字识别窗口 | 中低速通过，必要时短暂停车复核 |
| 转向阶段 | 先制动，再低速闭环转向 |
| 房间支路 | 低速进入，保证停车精度 |
| 返航已知路段 | 可比出发时略快，但仍保留路口减速 |

建议给 20 s 留出至少 20% 的裕量，将实车目标控制在约 15～16 s，而不是以 19.8 s 作为常态成绩。

优化顺序应为：

1. 减少识别等待；
2. 减少不必要的停车；
3. 提高直道巡航速度；
4. 缩短转向后的重新捕线时间；
5. 优化到门口的减速曲线；
6. 最后才提高转向速度。

---

## 12. 当前工程基础与题目需求差距

### 12.1 已有可复用能力

当前工程已经具备较好的底层基础：

| 当前文件 | 已有能力 | 对本题的价值 |
|---|---|---|
| `K230/digit_camera.py` | 使用 YOLO11 检测数字 1～8 | 可作为病房号识别起点 |
| `K230/main.py` | K230 摄像头、YOLO、UART 和目标坐标输出管线 | 可复用相机初始化、推理和串口框架 |
| `TICar/protocol_k230.c` | 非阻塞文本帧接收、校验、ACK、链路超时 | 可扩展为数字和路口信息协议 |
| `TICar/bsp_ccd.c` | 128 像素 CCD 采集和线位置识别 | 可用于高频循迹，但需验证红线与黑线干扰 |
| `TICar/bsp_motor.c` | 电机驱动、编码器、双轮速度闭环 | 可承担距离控制和直线速度控制 |
| `TICar/bsp_imu.c` | IMU 航向信息 | 可用于直行保持和 90° 转向 |
| `TICar/app_main.c` | 非阻塞模式调度、巡线、定距直线、方形路线状态机 | 可复用状态机、转向、制动和故障处理思想 |
| `TICar/bsp_gpio.c` | 板载 LED 和状态指示控制基础 | 可扩展红、黄、绿比赛指示灯 |

特别是现有方形路线模式已经包含：

- 编码器定距；
- 速度闭环；
- 转向粗调和重新找线；
- 制动等待；
- 丢线恢复；
- 速度故障确认；
- 转向超时和故障锁存。

这些机制与本题的“直线—路口—转向—重新循迹”高度相关，但比赛路线不能直接用固定方形循环代替，需要改造成由目标病房和动态路口识别驱动的任务状态机。

### 12.2 当前主要缺口

| 缺口 | 当前情况 | 后续工作 |
|---|---|---|
| 数字识别结果输出 | `digit_camera.py` 目前主要用于实时显示，尚未向 MSPM0 输出数字和方位 | 将数字、位置、置信度和稳定状态接入 UART |
| K230 协议 | 当前主要支持 `T,error_x,error_y,confidence` 和 `N` | 增加数字帧、路口帧、模式命令和序号 |
| 比赛任务状态机 | 现有模式以调试和固定路线为主 | 新增送药任务全流程状态机 |
| 红线识别 | 当前 CCD 算法主要按灰度线处理 | 实测红线、黑墙和白底灰度，必要时视觉融合 |
| 动态房间寻址 | 尚无 3～8 号随机映射逻辑 | 建立拓扑节点和路口数字匹配 |
| 门口到达检测 | 尚无比赛门口判定 | 结合里程、线路终点和视觉特征 |
| 载荷检测 | 当前未发现装药/卸药传感器接口 | 增加传感器硬件、BSP 和去抖逻辑 |
| 比赛指示灯 | 当前只有板载心跳/红色角点指示 | 增加独立红、黄、绿灯并绑定停车互锁 |
| 双车无线通信 | 当前工程未发现相应模块 | 选型并实现可靠消息和路权状态机 |
| 第二辆小车 | 当前仓库主要体现单车控制 | 复制硬件、独立标定并增加车号配置 |
| 完整场地测试 | 当前路线参数来自其他赛道调试 | 按 2021 F 题尺寸搭建场地重新标定 |

---

## 13. 推荐开发路线

### 阶段 0：场地和机械确认

- 按题目尺寸搭建至少一套完整或分段测试场地；
- 确认车宽、轴距和转向扫掠范围；
- 确认摄像头能看到数字标识区域；
- 确认托盘装载 200 g 后重心稳定；
- 准备两套 1～8 号数字纸张。

### 阶段 1：单车基础闭环

- 完成载荷传感器；
- 完成红、黄、绿灯输出和运动互锁；
- 完成药房识别目标数字；
- 完成装载后自动出发；
- 完成病房卸载后自动返航；
- 先用固定路线完成 1、2 号。

### 阶段 2：路口和动态数字

- 建立路口预期距离窗口；
- K230 输出数字值和左右方位；
- 完成中部两侧动态号码判断；
- 对误识别、漏识别、重复检测设计处理策略；
- 完成 3～8 号随机重排测试。

### 阶段 3：远端路线

- 完成远端中央路口左右决策；
- 完成侧走廊上/下决策；
- 记录完整出发路径；
- 逆序返航；
- 优化最长路线时间。

### 阶段 4：可靠性

- 多种顶灯和自然光测试；
- 满电、半电状态测试；
- 药品不同摆放位置测试；
- 数字纸轻微倾斜、偏移测试；
- 地面接缝和轻微褶皱测试；
- 连续运行统计成功率。

### 阶段 5：第二辆车和无线通信

- 先让第二辆车独立通过全部单车基本项；
- 增加车号和任务号；
- 实现可靠通信；
- 实现暂停点；
- 实现路权释放；
- 最后进行双车时间优化。

---

## 14. 建议测试矩阵

### 14.1 数字识别测试

至少覆盖：

- 数字 1～8 各类别；
- 正对、左偏、右偏；
- 不同距离；
- 自然光、顶灯、阴影；
- 单个数字和远端两个相邻数字；
- 不同打印批次；
- 运动状态下识别；
- 数字短时被车体或结构遮挡。

记录指标：

- 单帧准确率；
- 稳定确认时间；
- 漏检率；
- 误检率；
- 左右方位判断正确率；
- 各数字之间的混淆情况。

### 14.2 运动控制测试

- 直线 60 cm 距离误差；
- 40 cm 支路停车误差；
- 空载和 200 g 载荷速度差；
- 左转和右转角度误差；
- 转向后重新捕线时间；
- 满电和低电量下的速度一致性；
- 车体离黑线最小安全距离。

### 14.3 完整任务测试

建议为每个测试保存：

| 字段 | 示例内容 |
|---|---|
| 目标病房 | 1～8 |
| 房间随机布局 | 记录 3～8 的现场位置 |
| 运输时间 | 从装载完成到病房停车 |
| 返回时间 | 从卸载完成到药房停车 |
| 数字确认耗时 | 药房识别时间 |
| 转向次数 | 实际路线中的转向数量 |
| 最小边界距离 | 车体与黑线最小间距 |
| 识别重试次数 | 途中低速或停车复扫次数 |
| 是否压线 | 是/否及持续距离 |
| 是否人工干预 | 必须为否 |
| 结果 | 成功、失败及失败原因 |

不要只记录最快成绩，应同时统计：

- 平均时间；
- 最大时间；
- 最小时间；
- 连续成功次数；
- 失败模式分布。

比赛每项只测试一次，因此“连续成功率”比“偶尔一次最快成绩”更重要。

---

## 15. 主要风险与对策

| 风险 | 可能后果 | 推荐对策 |
|---|---|---|
| 红线和黑墙在 CCD 中灰度接近 | 跟错线、压墙线 | 限制视场、颜色视觉融合、里程窗口 |
| 数字识别错误 | 进入错误病房 | 多帧确认、只匹配目标数字、拓扑约束 |
| 识别过慢 | 超过 20 s 或运输时间变长 | 提前进入识别窗口、优化模型和曝光 |
| 固定延时转向漂移 | 转向后丢线或越界 | 编码器/IMU 粗转 + 视觉细对准 |
| 药品颠簸导致卸载误判 | 途中提前返航 | 状态约束、稳定时间、迟滞阈值 |
| 停车惯性过大 | 车头越过门口或压线 | 提前减速、主动制动、分段速度曲线 |
| 亮灯时车仍在运动 | 违反硬性规则 | 灯光与零速度互锁 |
| 通信消息丢失 | 小车 2 不启动或两车冲突 | ACK、序号、重发、超时停车 |
| 两车定位状态不一致 | 碰撞、错车 | 传输拓扑路段状态，不只传输时间 |
| 第二辆车参数直接复制 | 左右偏差和停车误差 | 每车独立保存标定参数 |
| 过度追求速度 | 压线、漏识别、转向失败 | 先达到高成功率，再逐段提速 |

---

## 16. 设计报告应准备的内容

设计报告占 20 分，应在开发过程中同步收集数据。

### 16.1 方案论证

应比较并说明：

- CCD 循迹、摄像头循迹或融合循迹；
- 数字分类和目标检测；
- 固定延时转向、编码器转向和 IMU 转向；
- 微动开关、压力传感器和称重方案；
- 单纯命令通信和带路权的可靠通信。

### 16.2 理论分析与计算

题目明确要求重点分析：

- 数字识别方法；
- 自动寻径方法。

还建议补充：

- 差速底盘运动关系；
- 编码器脉冲到距离的换算；
- 90° 转向轮程估算；
- PID 控制结构；
- 速度与 20 s 时间限制的关系；
- 车宽和走廊安全余量；
- 双车路权模型。

### 16.3 电路与程序设计

应包含：

- 总体硬件框图；
- 电机、编码器、IMU、CCD、K230、载荷、无线和指示灯接口；
- 电源分配和地线设计；
- 主任务状态机；
- 路口决策流程；
- K230—MSPM0 通信协议；
- 双车无线协议；
- 故障保护逻辑。

### 16.4 测试方案和结果

应给出：

- 测试场地和照明条件；
- 各病房多次运输/返回时间；
- 数字识别准确率；
- 停车误差；
- 不同载荷和电量下的结果；
- 双车任务时序；
- 失败案例和改进过程。

---

## 17. 最终结论

本题应按照以下技术主线推进：

> **先利用现有 MSPM0 底盘闭环和 CCD/IMU 能力完成可靠的单车循迹、转向和停车，再把 K230 的 1～8 数字检测接入正式串口协议，实现随机病房动态寻址；最后增加载荷、三色灯和双车无线路权状态机。**

当前工程已经具备电机、编码器、速度闭环、IMU、CCD、K230 推理框架、串口协议和非阻塞状态机等关键基础。距离完整完成 2021 F 题，最需要补齐的是：

1. 数字检测结果正式接入底盘；
2. 红线与黑墙环境下的稳定循迹；
3. 动态房间拓扑和返航路径；
4. 装药/卸药检测；
5. 独立红、黄、绿指示灯及停车互锁；
6. 第二辆车和可靠无线通信；
7. 按原题尺寸进行完整场地标定与连续成功率测试。

开发策略应始终坚持：

> **可靠性优先于极限速度，单车完整闭环优先于双车协同，真实随机场地测试优先于固定路线演示。**
